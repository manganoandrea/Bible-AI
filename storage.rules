rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read for story assets
    match /stories/{storyId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
